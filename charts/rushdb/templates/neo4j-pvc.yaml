{{- if and .Values.neo4j.enabled .Values.neo4j.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "rushdb.neo4j.fullname" . }}
  labels:
    {{- include "rushdb.neo4j.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.neo4j.persistence.accessMode }}
  resources:
    requests:
      storage: {{ .Values.neo4j.persistence.size }}
  {{- if .Values.neo4j.persistence.storageClass }}
  storageClassName: {{ .Values.neo4j.persistence.storageClass }}
  {{- end }}
{{- end }}
